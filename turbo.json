{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".wrangler/**", "build/**"]
		},
		"deploy": {
			"dependsOn": ["build"],
			"outputs": []
		},
		"test": {
			"outputs": []
		}
	}
}
